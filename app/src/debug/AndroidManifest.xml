<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-only manifest override, merged into the main one automatically by AGP
  for debug builds. Allows cleartext (non-TLS) traffic to 10.0.2.2, which the
  Firebase Auth/Firestore emulators require (see BiblioNetApp.EMULATOR_ENABLED).
  Never applies to a release build.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application android:networkSecurityConfig="@xml/network_security_config_debug" />
</manifest>
