rules_version = '2';

// Emulator-only rules for local demo data — never deployed to a real project.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
